// Prompt for Copilot
// Write a function called addNumbers that takes two parameters and returns their sum.
// For example, addNumbers(5, 3) should return 8.

function addNumbers(a, b) {
    return a + b;
}
